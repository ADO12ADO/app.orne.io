[build]
	command = "npx pnpm install --store=node_modules/.pnpm-store && node bin/build.js"

[build.environment]
	NODE_VERSION = "18"
	NPM_FLAGS = "--version" # prevent Netlify npm install

[[redirects]]
	from = "/*"
	to = "index.html"
	status = 200
